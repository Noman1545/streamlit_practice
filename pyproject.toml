[project]
name = "streamlit-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "noman asim", email = "asimnoman30@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.64.0",
]

[project.scripts]
streamlit-practice = "streamlit_practice:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
